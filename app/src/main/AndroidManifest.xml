<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gaby.tplabv">

    <application
        android:allowBackup="true"
        android:icon="@drawable/smile_icono"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".lista.ListaActivity" />
        <activity android:name=".categoria.CategoriaActivity" />
        <activity android:name=".registro.RegistroActivity"></activity>
    </application>

</manifest>